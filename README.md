# Estudo de Requisições AJAX com JavaScript

Este repositório contém um exercício prático desenvolvido durante um curso de Desenvolvimento Web. O objetivo foi compreender o funcionamento de requisições assíncronas sem o uso de bibliotecas externas ou a API Fetch moderna, focando na base do funcionamento da web.

## 🚀 Funcionalidades

- Navegação entre "páginas" sem recarregamento (Single Page Application concept).
- Implementação de **AJAX** utilizando o objeto `XMLHttpRequest` nativo.
- Manipulação do **DOM** para injetar conteúdo dinamicamente.
- Tratamento de estados da requisição (`readyState` e `status`).
- Feedback visual de carregamento (Loading spinner).
- Tratamento de erro básico (404).

## 🛠️ Tecnologias Utilizadas

- **HTML5**: Estrutura da página.
- **Bootstrap 4**: Estilização e layout responsivo.
- **JavaScript (Vanilla)**: Lógica de requisição e manipulação de resposta.

## 📝 O que aprendi

Com este projeto, pratiquei conceitos fundamentais de como o navegador se comunica com o servidor em segundo plano, entendendo o ciclo de vida de uma requisição HTTP antes de partir para abstrações mais modernas como `fetch` ou `Axios`.

---
*Desenvolvido para fins de estudo.*
